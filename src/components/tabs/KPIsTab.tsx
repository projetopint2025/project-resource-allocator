import { type Project } from "@/types";

export function KPIsTab({ project }: { project: Project }) {
  return (
    <div className="p-4">
      <h2>KPIs</h2>
      {/* Add KPI content here */}
    </div>
  );
}
