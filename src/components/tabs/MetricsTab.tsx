import { type Project } from "@/types";

export function MetricsTab({ project }: { project: Project }) {
  return (
    <div className="p-4">
      <h2>Metrics</h2>
      {/* Add metrics content here */}
    </div>
  );
}
